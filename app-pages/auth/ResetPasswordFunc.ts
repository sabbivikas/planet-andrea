/**
 * Reset Password Func that handles password recovery
 */
import { ResetPasswordProps } from '@/app/auth/reset-password';

/**
 * Interface for the return value of the useResetPassword hook
 */
export interface ResetPasswordFunc {}

/**
 * Custom hook that provides business logic for the ResetPassword component
 *
 */
export function useResetPassword(props: ResetPasswordProps): ResetPasswordFunc {
  return {};
}
